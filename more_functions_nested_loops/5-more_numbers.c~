#include "main.h"

/**
 * more_numbers - A function that prints numbers 0 to 14.
 * @c : 1st operand
 * @i : 2nd operand
 */
void more_numbers(void)
{
char c;
char i;

while (i = '0'; i <= '9'; i++)
{
for (c = '0'; c <= "14"; c++)
{
_putchar(c);
}
_putchar(i);
}
_putchar('\n');
}
