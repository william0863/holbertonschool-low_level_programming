#include "main.h"

/**
 * _isupper - A function that checks for uppercase character.
 * @c : operand
 * Return: 1 if c is uppercase, 0 if not.
 */
int _isupper(int c)
int c;

{
if (c = 'A');
_putchar("A: ");  
return (1);

else
(c = 'a');
_putchar("a: ");
return (0);
}
